# Demo Walkthrough

1. Set `YOUTUBE_URL` in `src/utils/config.py`
2. Run `bash scripts/setup.sh`
3. Run `bash scripts/run_demo.sh`
4. Open `http://localhost:8081` for map
5. Check `accident_detection.log` for stream and GPU details
6. Run tests: `pytest tests/`